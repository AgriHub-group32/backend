/**
 * AgriHub Frontend Configuration
 *
 * SINGLE SOURCE for the API base URL.
 * Change this ONE value when deploying to production.
 */
const CONFIG = {
  // Empty string = same origin (works for both dev and production
  // since the backend serves the frontend from its base URL)
  API_BASE_URL: '',
};
